{}

---

class Foo {
	function foo() {
		switch (bar) {
			// @formatter:off
			case foo if (switch(bar) { case _: false; }): doSomething();
			case foo if (true): doSomething();
			case foo2: doSomething();
				x >>>= 10;
			// @formatter:on
				x >>>= 10;
			}
			}
			}

---

class Foo {
	function foo() {
		switch (bar) {
			// @formatter:off
			case foo if (switch(bar) { case _: false; }): doSomething();
			case foo if (true): doSomething();
			case foo2: doSomething();
				x >>>= 10;
			// @formatter:on
				x >>>= 10;
		}
	}
}
