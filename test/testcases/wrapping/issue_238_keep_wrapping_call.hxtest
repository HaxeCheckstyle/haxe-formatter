{
	"wrapping": {
		"callParameter": {
			"defaultWrap": "keep",
			"rules": []
		}
	}
}

---

class Main {
	static function main():Void {
		g.drawImage(
			tileset.img,
			ix * tileSize,
			iy * tileSize,
			tileSize, tileSize
		);
	}
}

---

class Main {
	static function main():Void {
		g.drawImage(
			tileset.img,
			ix * tileSize,
			iy * tileSize,
			tileSize, tileSize
		);
	}
}
