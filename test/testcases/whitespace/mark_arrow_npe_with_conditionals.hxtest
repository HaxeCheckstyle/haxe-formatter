{}

---

function prepareToSong()
{
#if (target.threaded)
Thread.create(() -> {
#end
completedThread();
#if (target.threaded)
});
#end
}

---

function prepareToSong() {
	#if (target.threaded)
	Thread.create(() -> {
	#end
		completedThread();
	#if (target.threaded)
	});
	#end
}
