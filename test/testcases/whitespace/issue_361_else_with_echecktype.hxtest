{}


---

class Main {
	static function main() {
		return if (!Std.is(raw(), Array)) list(raw()) else (raw() : Array<T>);
	}
}

---

class Main {
	static function main() {
		return if (!Std.is(raw(), Array)) list(raw()) else (raw() : Array<T>);
	}
}
