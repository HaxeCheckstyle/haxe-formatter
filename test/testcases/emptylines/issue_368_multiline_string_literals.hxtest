{
}

---

class Main {
	static function main() {
		it("part1", {
			run(parse("

")).should.be(3);
		});
	}
}

---

class Main {
	static function main() {
		it("part1", {
			run(parse("

")).should.be(3);
		});
	}
}
