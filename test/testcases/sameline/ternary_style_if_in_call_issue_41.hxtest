{}

---

class Main {
	public static function main() {
		trace(if (Math.random() > 0.5) true else false);
	}
}

---

class Main {
	public static function main() {
		trace(if (Math.random() > 0.5) true else false);
	}
}
