{
	"sameLine": {
		"expressionIf": "same"
	}
}

---

class Main {
	function foo():Bool {
		return switch (tok) {
			case POpen:
				if (token) false;
				else true;
		}
	}
}

---

class Main {
	function foo():Bool {
		return switch (tok) {
			case POpen:
				if (token) false; else true;
		}
	}
}
